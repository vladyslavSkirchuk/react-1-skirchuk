export type TData = {
    id?: number
    name: string
    description: string
    imageUrl?: string
    contractTypeId?: number
    contractSignedOn?: string
    budget: number
    isActive?: boolean
}
